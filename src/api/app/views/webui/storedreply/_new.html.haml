- if !User.session
  .alert.alert-warning{ role: 'alert' }
    Login required, please
    = link_to('login', new_session_url)
    - if can_register?
      or
      = link_to('signup', new_user_url)
    in order to comment
- else
  = render(partial: 'webui/storedreply/comment_field',
   locals: { form_method: :post, storedreply: StoredReply.new})
